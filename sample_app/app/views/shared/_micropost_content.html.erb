<span class="content"><%= wrap(object.content) %></span>
<span class="timestamp">
  <%# time_ago_in_wordsメソッドは日時を「〜前」のように表示する %>
  Posted <%= time_ago_in_words(object.created_at) %> ago.
</span>
<% if current_user?(object.user) %>
  <%= link_to 'delete', object, method: :delete, data: { confirm: "You sure?" }, title: object.content %>
<% end %>