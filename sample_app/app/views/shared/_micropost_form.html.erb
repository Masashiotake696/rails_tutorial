<%= form_for(@micropost) do |f| %>
  <%#
    フォーム変数fをf.objectとすることによって、関連付けられたオブジェクトにアクセスすることができる。
    つまり、
    form_for(@user) do |f| の場合、f.object は @user となり、
    form_for(@micropost) do |f| の場合、f.object は @micropost となる
   %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new micropost...", maxlength: 140 %>
    <span id="remaining" style="text-align: right; color: red;">140</span>
  </div>

  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>